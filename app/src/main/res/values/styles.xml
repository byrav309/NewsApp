<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="titleStyle" parent="Base.TextAppearance.AppCompat.Large">
        <item name="android:layout_margin">@dimen/text_view_margin</item>
    </style>

    <style name="authorStyle" parent="Base.TextAppearance.AppCompat.Small">
        <item name="android:layout_margin">@dimen/text_view_margin</item>
    </style>

    <style name="dateStyle" parent="Base.TextAppearance.AppCompat.Small">
        <item name="android:layout_margin">@dimen/text_view_margin</item>
    </style>

    <style name="sectionStyle" parent="Base.TextAppearance.AppCompat.Small">
        <item name="android:gravity">right</item>
        <item name="android:layout_margin">@dimen/text_view_margin</item>
    </style>

</resources>
